[package]
name = "kafig-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
rquickjs = { version = "0.11.0", features = [
    "std",
    "classes",
    "properties",
    "array-buffer",
    "either",
    "loader",
    "macro",
] }
